services:
  - type: web
    name: dash-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn aplicacion:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
